name: Validate

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Basic tree check
        run: |
          test -f custom_components/adelaide_metro/manifest.json
          test -f hacs.json
