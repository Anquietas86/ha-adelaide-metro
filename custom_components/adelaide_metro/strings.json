{
  "config": {
    "step": {
      "user": {
        "title": "Adelaide Metro",
        "description": "Enter the route IDs you want to monitor (e.g. SEAFRD,GLNELG).\n\nStop IDs are optional — leave blank to track all stops on selected routes, or enter specific stop IDs to filter further.",
        "data": {
          "routes": "Route IDs (comma separated)",
          "stops": "Stop IDs (comma separated, optional)",
          "max_departures": "Maximum departures to keep",
          "refresh_interval": "Refresh interval in seconds",
          "expose_to_assistants": "Expose to assistants",
          "static_gtfs_refresh_hours": "Static GTFS refresh interval in hours"
        }
      }
    },
    "error": {
      "no_routes": "Enter at least one route ID (e.g. SEAFRD).",
      "invalid_max_departures": "Maximum departures must be at least 1.",
      "invalid_refresh_interval": "Refresh interval must be at least 15 seconds."
    },
    "abort": {
      "already_configured": "These routes are already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Adelaide Metro options",
        "description": "Edit routes, stops, update behavior, and assistant exposure.\n\nRoutes are required. Leave stops blank to auto-discover all stops on selected routes.",
        "data": {
          "routes": "Route IDs (comma separated)",
          "stops": "Stop IDs (comma separated, optional)",
          "max_departures": "Maximum departures to keep",
          "refresh_interval": "Refresh interval in seconds",
          "expose_to_assistants": "Expose to assistants",
          "static_gtfs_refresh_hours": "Static GTFS refresh interval in hours"
        }
      }
    },
    "error": {
      "no_routes": "Enter at least one route ID (e.g. SEAFRD).",
      "invalid_max_departures": "Maximum departures must be at least 1.",
      "invalid_refresh_interval": "Refresh interval must be at least 15 seconds."
    }
  }
}
